---
import Layout from '../../layouts/Layout.astro';
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);

const featured = posts.filter(p => p.data.featured);
const rest = posts.filter(p => !p.data.featured);
---

<Layout title="Blog — Duong Ngoc Vu">
  <Navbar />
  <main class="pt-28 pb-24 min-h-screen">
    <div class="max-w-4xl mx-auto px-6">

      <div class="mb-14" data-aos="fade-up">
        <p class="text-blue-400 font-mono text-sm font-medium mb-2">// writing</p>
        <h1 class="text-3xl sm:text-4xl font-bold text-white mb-3">Blog</h1>
        <p class="text-slate-400">Research notes, technical writing, and updates.</p>
      </div>

      {featured.length > 0 && (
        <div class="mb-12">
          <h2 class="text-xs font-semibold text-slate-500 uppercase tracking-widest mb-5">Featured</h2>
          <div class="grid sm:grid-cols-2 gap-5">
            {featured.map((post, i) => (
              <a
                href={`/NgocDuong.github.io/blog/${post.slug}`}
                class="group block p-6 rounded-2xl bg-[#0f172a] border border-blue-500/20 hover:border-blue-500/50 transition-all duration-300 hover:-translate-y-0.5 hover:shadow-xl hover:shadow-blue-500/5"
                data-aos="fade-up"
                data-aos-delay={i * 80}
              >
                <div class="flex flex-wrap gap-1.5 mb-3">
                  {post.data.tags.map(tag => (
                    <span class="text-xs px-2 py-0.5 rounded-full bg-blue-500/10 border border-blue-500/20 text-blue-400">
                      {tag}
                    </span>
                  ))}
                </div>
                <h3 class="font-bold text-white leading-snug mb-2 group-hover:text-blue-200 transition-colors">
                  {post.data.title}
                </h3>
                <p class="text-slate-400 text-sm leading-relaxed mb-4 line-clamp-2">
                  {post.data.description}
                </p>
                <div class="flex items-center justify-between">
                  <time class="text-xs text-slate-600 font-mono">
                    {post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' })}
                  </time>
                  <span class="text-blue-500 text-sm group-hover:translate-x-1 transition-transform inline-block">
                    Read &rarr;
                  </span>
                </div>
              </a>
            ))}
          </div>
        </div>
      )}

      {rest.length > 0 && (
        <div>
          <h2 class="text-xs font-semibold text-slate-500 uppercase tracking-widest mb-5">All Posts</h2>
          <div class="divide-y divide-slate-800/60">
            {rest.map((post, i) => (
              <a
                href={`/NgocDuong.github.io/blog/${post.slug}`}
                class="group flex items-start justify-between gap-4 py-5 hover:bg-blue-500/3 -mx-3 px-3 rounded-xl transition-colors"
                data-aos="fade-up"
                data-aos-delay={i * 60}
              >
                <div class="flex-1 min-w-0">
                  <div class="flex flex-wrap gap-1.5 mb-1.5">
                    {post.data.tags.map(tag => (
                      <span class="text-xs px-2 py-0.5 rounded-full bg-slate-800 border border-slate-700/50 text-slate-400">
                        {tag}
                      </span>
                    ))}
                  </div>
                  <h3 class="font-semibold text-white group-hover:text-blue-300 transition-colors">
                    {post.data.title}
                  </h3>
                  <p class="text-sm text-slate-500 mt-1 line-clamp-1">{post.data.description}</p>
                </div>
                <time class="text-xs text-slate-600 font-mono flex-shrink-0 mt-1">
                  {post.data.date.toLocaleDateString('en-US', { year: 'numeric', month: 'short' })}
                </time>
              </a>
            ))}
          </div>
        </div>
      )}

    </div>
  </main>
  <Footer />
</Layout>
